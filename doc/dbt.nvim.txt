*dbt.nvim.txt* Utilities for working with dbt-core projects from within neovim
                                 For NVIM v0.8.0    Last change: 2026 April 17

==============================================================================
Table of Contents                                 *dbt.nvim-table-of-contents*

1. dbt.nvim                                                |dbt.nvim-dbt.nvim|
2. Installation                                        |dbt.nvim-installation|
3. Usage                                                      |dbt.nvim-usage|
  - dbt commands                                 |dbt.nvim-usage-dbt-commands|
  - dbt show                                         |dbt.nvim-usage-dbt-show|
  - Navigation                                     |dbt.nvim-usage-navigation|
  - Code generation                           |dbt.nvim-usage-code-generation|

==============================================================================
1. dbt.nvim                                                *dbt.nvim-dbt.nvim*

Simple utilities to facilitate working in dbt-core projects from within neovim.


==============================================================================
2. Installation                                        *dbt.nvim-installation*

Using lazy.nvim <https://github.com/folke/lazy.nvim>:

>lua
    {
      "EloiSanchez/dbt.nvim",
      dependencies = {
        "nvim-lua/plenary.nvim",
      },
    }
<


==============================================================================
3. Usage                                                      *dbt.nvim-usage*


DBT COMMANDS                                     *dbt.nvim-usage-dbt-commands*

dbt commands can be executed from the neovim command line with the following
syntax (example with `dbt build`)

>bash
    :DbtBuild [[@|+]dbt_model_selection][*]]
<

for instance

>bash
    :DbtBuild orders+,@customers
<

will open a docked window where the results of the dbt execution are shown.
This window can be closed by pressing `q` and can be reopened without losing
information about past executions with `:DbtOpenTerm`.

  Available dbt commands
  ------------------------
  dbt build
  dbt run
  dbt compile
  dbt seed
  dbt test

USING % FOR CURRENT BUFFER ~

The special character `%` will be replaced by the current’s buffer model
name. Therefore, if current buffer is `./models/silver/employees.sql` then:

>bash
    :DbtBuild %+
<

is translated to

>bash
    :DbtBuild employees+
<


DBT BUILD ~

`:DbtBuild [dbt_model_selection]`: Executes `dbt build`


DBT RUN ~

`:DbtRun [dbt_model_selection]`: Executes `dbt run`


DBT COMPILE ~

`:DbtCompile [dbt_model_selection]`: Executes `dbt compile`


DBT SEED ~

`:DbtSeed [dbt_model_selection]`: Executes `dbt seed`


DBT TEST ~

`:DbtTest [dbt_model_selection]`: Executes `dbt test`


OPEN TERMINAL ~

`:DbtOpenTerm`: Opens docked window showing past executions of
|dbt.nvim-dbt-commands|


DBT SHOW                                             *dbt.nvim-usage-dbt-show*

`:{Visual}DbtShow`

Executes the _current_ buffer code against the database and show the results.

If used in visual mode, only the selected range is executed.

The automatically opened docked window can be closed by pressing `q` in it and
can be reopened later without losing the previous results with
`:DbtOpenShowResults`.

`:DbtOpenShowResults`

Opens docked window showing past executions of |dbt.nvim-:dbtshow|.


NAVIGATION                                         *dbt.nvim-usage-navigation*

`:DbtGoToDefinition`: Jump to the model under the cursor referenced by a `{{
ref(...) }}` snippet.

`:DbtGoToReferences`: Opens quickfix list of references to model opened in
current buffer.


CODE GENERATION                               *dbt.nvim-usage-code-generation*

`:DbtGenerateModelYaml [yaml_save_path]`: Open a buffer with yaml definition of
current model. If `yaml_save_path` is passed, the buffer will be created on the
specified path, ready to be written. Otherwise, the generated yaml is opened on
a scratch buffer, where it can be yanked and closed (or |CTRL-O| to jump to
previous location).

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
